type User {
  githubLogin: ID!
  name: String
  avatar: String
}

type AuthPayload {
  token: String!
  user: User!
}

type Query {
  me: User
  totalUsers: Int!
  allUsers: [User!]!
}

type Mutation {
  githubAuth(code: String!): AuthPayload!
}
